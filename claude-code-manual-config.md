# 2025-11-05 Claude Code手动配置避坑指南

## 爆款标题
手动搞定Claude Code？这份Mac实战攻略让我一晚配好AI写码搭子

## 爆款正文
今天亲测 Claude Code 手动配置，全程踩点记录给大家：截图里这份脚本就是核心，先确认 Node.js + npx 按图索骥装好，终端跑起来才能让 Claude 接住你的指令。把 API Token、代理地址、目标端点逐项换成自己的，末尾记得 `moonshot-v1-8k` 也可以替换成想用的模型。  
图中绿色✅写得很清楚——测通过的端点、兼容性、格式转换都OK，再也不用担心回复乱码。小技巧：先运行 `claude-start` 测一遍，确保本地 Kimi K2 或官方模型都能接管，再执行 `npx @anthropic-ai/claude-code` 就能一键唤醒。  
我还额外加了 `claude-code-router` 做兜底，遇到 Claude 官方限流时自动切换 Kimi K2，不担心半夜断连。整套流程一小时搞定，从此 Claude 变成随叫随到的远程pair，一边写代码一边给你提示。要不要也试试把 AI 写码机变成你的私人大牛？  
#ClaudeCode #Mac配置 #AI工作流 #手把手教程

## 朋友圈文案
今晚史上最折腾的 Claude Code 终于被我征服了！Node.js、npx 一条条检查，API Token、代理地址一个个替换，`claude-start` + `npx @anthropic-ai/claude-code` 连招打下来，Mac 上的 Claude 总算乖乖接管我的代码。  
还顺手装了 `claude-code-router` 做备用线路，遇到官方限流直接切回 Kimi K2，半夜也不会断联。以后写代码再也不用孤军奋战，随时叫 Claude 上场陪聊陪写。  
准备再撸一个 demo，把这套流程打包分享给大家，想试的随时喊我。
